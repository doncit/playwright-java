# Purchase plan with invalid data Test

## Required tools

* Java >=17
* Maven >=3.8

## Run

Create a `secrets.yaml` file in the repository root. Use the `secrets-example.yaml` as an example. Fill in your username and password.

Then, run the test with:

```shell
mvn test
```
